---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# metadata: name and namespace are not needed at the top-level repo kustomization
resources:
  - ./git
  - ./helm
  - ./oci
  - ./entries
