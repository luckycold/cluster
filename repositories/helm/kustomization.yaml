apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
    name: helm-repos
    namespace: flux-system
resources:
  # Mandatory repositories from clustertool v2 article
  - bjw-s.yaml
  - cloudnative-pg.yaml
  - home-ops-mirror.yaml
  - jetstack.yaml
  - prometheus-community.yaml
  - spegel.yaml
  - truecharts.yaml
  # Add any other .yaml files present in this directory (cluster/repositories/helm/)
  # that define HelmRepository CRDs and you want to include.
  # For example, if you have custom ones or ones not in the default list.
  # Ensure all listed files exist in this directory.

  # From your existing files, we can also include:
  - csi-driver-nfs.yaml
  - descheduler.yaml
  - intel.yaml
  - nvidia.yaml
# more goes here!
