apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flaresolverr-config-longhorn
  namespace: flaresolverr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
