---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml # Add namespace definition first
  - helm-release.yaml

# Enable variable substitution using the config file created above
# configurations:
# - kustomizeconfig.yaml 