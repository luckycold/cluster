apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgadmin-config-longhorn
  namespace: pgadmin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
