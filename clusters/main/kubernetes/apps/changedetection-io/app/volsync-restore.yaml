apiVersion: v1
kind: Secret
metadata:
  name: changedetection-io-datastore-volsync-minio
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: ${VOLSYNC_BACKUP_MAIN_ACCESSKEY}
  AWS_SECRET_ACCESS_KEY: ${VOLSYNC_BACKUP_MAIN_SECRETKEY}
  RESTIC_PASSWORD: ${VOLSYNC_BACKUP_MAIN_ENCRKEY}
  RESTIC_REPOSITORY: s3:${VOLSYNC_BACKUP_MAIN_URL}/${VOLSYNC_BACKUP_MAIN_BUCKET}/changedetection-io/volsync/datastore-volsync-minio
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: changedetection-io-datastore-minio-dest
spec:
  trigger:
    manual: restore-once
  restic:
    repository: changedetection-io-datastore-volsync-minio
    destinationPVC: changedetection-io-datastore
    copyMethod: Direct
    cacheCapacity: 10Gi
    cleanupCachePVC: false
    accessModes:
      - ReadWriteOnce
    moverSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
