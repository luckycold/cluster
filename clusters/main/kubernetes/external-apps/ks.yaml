apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: external-apps
    namespace: flux-system
spec:
    interval: 10m
    path: clusters/main/kubernetes/external-apps/app
    prune: true
    sourceRef:
        kind: GitRepository
        name: cluster
